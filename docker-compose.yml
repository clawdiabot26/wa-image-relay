services:
  wa-image-relay:
    build: .
    container_name: wa-image-relay
    restart: unless-stopped
    ports:
      - "8087:8087"
    volumes:
      - wa-auth:/app/auth
    environment:
      - NODE_ENV=production

volumes:
  wa-auth:
